{
  "name": "evia-pwa",
  "version": "0.30.0",
  "private": true,
  "scripts": {
    "check": "node --check app.js && node --check calculations.js && node --check ev-models.js && node --check service-worker.js",
    "test": "node --test tests/*.test.js",
    "build:native": "node scripts/prepare-native.mjs",
    "cap:sync": "npm run build:native && cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/ios": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
